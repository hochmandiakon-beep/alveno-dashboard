services:
  - type: web
    name: alveno-manager-hub
    runtime: node
    buildCommand: echo "No build needed"
    startCommand: node server.js
    plan: free
    envVars:
      - key: ALVENO_TOKEN
        sync: false
